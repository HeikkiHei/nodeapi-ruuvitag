{
  "name": "nodeapi-ruuvitag",
  "version": "0.5.0",
  "description": "Node.js API for communicating with RuuviTag BLE Sensor Beacon and decoding data to JSON",
  "main": "index.js",
  "scripts": {
    "start": "sudo node index.js",
    "dev": "sudo nodemon --exec 'node index.js -v'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JHu",
  "license": "MIT",
  "dependencies": {
    "@abandonware/noble": "^1.9.2-7",
    "dateformat": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
